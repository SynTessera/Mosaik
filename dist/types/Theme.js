export var ColorMode;
(function (ColorMode) {
    ColorMode["dark"] = "dark";
    ColorMode["light"] = "light";
})(ColorMode || (ColorMode = {}));
export const COLOR_MODES = [ColorMode.dark, ColorMode.light];
